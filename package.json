{
  "name": "adventure",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "make_jpg": "node make_jpg.js",
    "dev": "npm run make_jpg && vite",
    "build": "npm run make_jpg && vite build",
    "preview": "npm run make_jpg && vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "adventurejs": "^1.0.1",
    "jimp": "^0.22.10"
  }
}